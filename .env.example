# XUM Portal environment configuration
# Copy to .env.local and adjust per environment.

# URL of the user-facing XUM AI web app ("Sign in online" destination).
# Dev default (if unset): http://localhost:3000
# Production example: https://app.xumai.app
VITE_APP_URL=http://localhost:3000
